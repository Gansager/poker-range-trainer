# Poker Range Trainer — Требования (ТЗ)

> Веб-приложение для тренировки префлоп-диапазонов в покере.
> Пользователь выбирает действия для рук в матрице 13×13, нажимает **Check**, и приложение
> сравнивает ответ с эталонным диапазоном, подсвечивая несоответствия.

Дата: 2026-06-04
Версия документа: 0.1 (черновик)

---

## 1. Цель и ценность

Создать тренажёр, который помогает запомнить и отрабатывать префлоп-диапазоны
(на базе чартов FunFarm: Открытие, VPIPvs1r, BBdef, BvB, ISOvsFish).

**Основной цикл тренировки:**
1. Пользователь выбирает категорию и конкретный спот (например, `VPIPvs1r → vsCO`).
2. Видит пустую матрицу 13×13 (169 рук).
3. Назначает рукам действия (3бет / колл / фолд / изолейт и т.д.), при необходимости — частоты.
4. Нажимает **Check**.
5. Приложение подсвечивает: ✅ совпавшие, ❌ ошибочные, ⚠️ пропущенные руки. Показывает точность (%).

**Не-цели (на старте):**
- Постфлоп-тренировка.
- Реальная раздача карт / симуляция стола.
- Мультиплеер, аккаунты, сервер (всё работает локально в браузере).

---

## 2. Решения по проекту (зафиксировано)

| Вопрос | Решение |
|---|---|
| Режим тренировки | **Действия + частоты** (смешанные стратегии: 3бет 40% / колл 60% и т.п.) |
| Стек | **React + Vite + TypeScript** |
| Источник данных | **Встроенный редактор диапазонов** (пользователь рисует эталоны мышью и сохраняет) |
| Backend | Нет. Чистый клиент, данные в `localStorage` + экспорт/импорт JSON |
| Заливка смешанных частот | **Диагональные полосы** (как на чартах FunFarm) |
| Ввод частот | **Слайдер на действие** (сумма авто-нормализуется к 100%) |
| Палитра цветов | **Точь-в-точь как FunFarm** |
| Таймер на спот | Не нужен |

---

## 3. Глоссарий и доменная модель

### 3.1 Матрица рук (13×13 = 169 рук)
Стандартная сетка:
- Ряды и колонки: ранги `A K Q J T 9 8 7 6 5 4 3 2`.
- **Диагональ** — карманные пары (AA, KK, … 22).
- **Верхний правый треугольник** — одномастные руки (suited), например `AKs`.
- **Нижний левый треугольник** — разномастные (offsuit), например `AKo`.
- Всего: 13 пар + 78 suited + 78 offsuit = **169 рук**.

Каждая рука имеет канонический ID: `AA`, `AKs`, `AKo`, `72o` и т.д.

### 3.2 Действия (Actions)
Набор действий зависит от категории спота. Базовый каталог с цветами (по чартам):

| Action ID | Подпись | Цвет (примерно) |
|---|---|---|
| `fold` | Фолд | белый / прозрачный |
| `open` / `raise` | Опенрейз / Рейз | красный |
| `call` | Колл / колдкол / лимп-колл | зелёный |
| `3bet` | 3бет | красный/розовый |
| `4bet` | 4бет | фиолетовый |
| `limp` | Лимп | зелёный (св.) |
| `iso_small` | Изолейт 3.5bb | светло-красный |
| `iso_big` | Изолейт 5bb+ | тёмно-красный |
| `overlimp` | Оверлимп | жёлтый |
| `check` | Чек | зелёный |
| `limp_fold` | Лимп-фолд | — |

> Точный набор и цвета настраиваются per-category (см. §6). Цвета должны совпадать с чартами FunFarm, чтобы визуальная память переносилась.

### 3.3 Частота (Frequency) и смешанная стратегия
Каждая ячейка матрицы хранит **взвешенное распределение действий**, в сумме = 100%:
```
"AKs": { "3bet": 100 }
"A5s": { "3bet": 35, "call": 65 }
"99":  { "3bet": 100 }
"K9s": { "call": 96, "fold": 4 }
```
Чистая рука = одно действие 100%. Смешанная = ≥2 действий.

### 3.4 Спот (Spot)
Конкретная тренируемая ситуация = одна матрица 169 ячеек + метаданные:
- `id`, `name` (напр. `vsCO 38`), `category`, набор допустимых действий, заметки/рекомендации.

### 3.5 Категория (Category) = таб в UI
Группа спотов одного типа (Открытие, VPIPvs1r, BBdef, BvB, ISOvsFish).

---

## 4. Модель данных (JSON-схема)

```jsonc
// ranges.json — корневая структура
{
  "version": 1,
  "categories": [
    {
      "id": "vpip_vs_1r",
      "name": "VPIP vs 1 raiser (50bb)",
      "actions": ["3bet", "call", "fold"],   // допустимые действия в категории
      "spots": [
        {
          "id": "co_vs_btn",
          "name": "vsBTN 55",
          "notes": "Сайзинги 3бетов: 3x на свободных, 4x с блайндов...",
          "matrix": {
            "AA":  { "3bet": 100 },
            "AKs": { "3bet": 100 },
            "A5s": { "3bet": 35, "call": 65 },
            "72o": { "fold": 100 }
            // ... 169 ключей; отсутствующий ключ трактуется как { "fold": 100 }
          }
        }
      ]
    }
  ]
}
```

**Правила:**
- Сумма частот в ячейке = 100 (валидация в редакторе).
- Отсутствующая в `matrix` рука = `fold 100%` (для компактности храним только не-фолд руки).
- 169 канонических ключей рук генерируются кодом из массива рангов.

---

## 5. Каталог категорий и спотов (из чартов FunFarm)

> Эталонные данные вводятся через редактор. Ниже — целевой список спотов.

### 5.1 Открытие (RFI / Open)
Действия: `open`, `fold`.
Споты по позициям: **EP, MP, HJ, CO, BTN, SB**.
Дополнительно (фаза 2): **RFI vs 3bet** — защита опенрейзера (`call3bet`, `4bet`, `fold`) для EP/MP/HJ/CO/BTN.

### 5.2 VPIPvs1r — против 1 рейза, 50bb deep
Действия: `3bet`, `call`, `fold`.
Споты (герой vs позиция открывшего):
- Из EP/HJ: `EPvsEP`, `HJvsEP`, `HJvsMP`
- Из CO: `vsEP`, `vsMP`, `vsHJ`
- Из BTN/SB: `vsEP`, `vsMP`, `vsHJ`, `vsCO`, `vsBTN`

### 5.3 BBdef — защита BB против разных сайзов
Действия: `3bet`, `call`, `fold`.
Споты = позиция открывшего × сайзинг:
- Сайзинги: **2.0bb, 2.5bb, 3.0bb**
- Позиции: `vsEP, vsMP, vsHJ, vsCO, vsBTN`
- Итого до 15 спотов (можно начать с одного ряда сайзингов).

### 5.4 BvB — Blind vs Blind
Несколько под-спотов с разными наборами действий:
- **SB VPIP**: `raise`, `limp`, `fold` (VPIP 100%, Raise 50% / Limp 50%)
- **SB vs 3bet** (после открытия рейзом): `call`, `4bet`, `fold`
- **SB limp → получил iso 3.5**: `limp_call`, `limp_fold`
- **BB vs limp SB**: `iso`, `check`
- **BB vs raise SB**: `3bet`, `call`, `fold`

### 5.5 ISOvsFish — изолейт против 1 лимпера-любителя
Действия: `iso_small` (3.5bb), `iso_big` (5bb+), `overlimp`, `fold`.
Споты по позициям: `EP, MP, HJ, CO, BTN, SB+BB vs1limp`, плюс `vslimp+overlimp`.

---

## 6. Функциональные требования

### 6.0 Палитра цветов (референс FunFarm)
Дефолтные цвета действий. Точные hex уточняются пипеткой по скриншотам на этапе вёрстки; ниже — стартовые приближения:

| Action | Подпись | Цвет (approx hex) |
|---|---|---|
| `fold` | фолд | `#FFFFFF` (рамка `#CFCFCF`) |
| `open` / `raise` / `3bet` | опенрейз / рейз / 3бет | `#EF9A9A` (розово-красный) |
| `iso_small` | изолейт 3.5bb | `#F4A6A6` (светло-красный) |
| `iso_big` | изолейт 5bb+ | `#E53935` (тёмно-красный) |
| `call` / `limp` / `check` | колл / колдкол / лимп / чек | `#A5D6A7` (зелёный) |
| `4bet` | 4бет | `#B39DDB` (фиолетовый) |
| `overlimp` | оверлимп | `#FFF59D` (жёлтый) |

> Смешанная ячейка заливается диагональными полосами этих цветов, ширина ∝ частоте.

### 6.1 Навигация
- **FR-1.** Верхние табы = категории (§5). Внутри категории — список/сетка спотов (кнопки или выпадающий список).
- **FR-2.** Состояние выбранного спота сохраняется при переключении (или сбрасывается — настраивается).

### 6.2 Матрица 13×13 (центральный компонент)
- **FR-3.** Отрисовка 169 ячеек с подписями рук, ряды/колонки A→2.
- **FR-4.** Раскраска ячейки по назначенному действию. Для смешанных частот — **диагональная/долевая заливка** двумя+ цветами пропорционально частотам (как на чартах).
- **FR-5.** Подпись частоты в ячейке (напр. «35%») — опционально, тумблер «показывать частоты».
- **FR-6.** Hover по ячейке показывает руку + назначенное распределение.

### 6.3 Режим тренировки (Training)
- **FR-7.** При старте спота матрица пустая (всё `fold`/нейтральное).
- **FR-8.** Выбор активного действия — палитра кнопок (по `actions` категории) + горячие клавиши.
- **FR-9.** Назначение действия:
  - клик по ячейке = красит активным действием;
  - drag-выделение прямоугольником/линией = массовое назначение (важно для скорости).
  - повторный клик тем же действием = снять (вернуть в fold).
- **FR-10.** Поддержка частот: для активного действия можно задать % (слайдер/поле), либо «paint mode» 100%. Минимум — назначить долю активного действия, остаток уходит в `fold` или предыдущее действие.
- **FR-11.** Кнопка **Check** — запускает сравнение и показывает результат.
- **FR-12.** Кнопки **Reset** (очистить), **Show answer** (показать эталон), **Next spot** / **Random spot**.

### 6.4 Логика проверки (Check) — ключевая
Сравнение ответа пользователя с эталоном по каждой из 169 рук. Поддержать **режимы строгости** (настройка):

- **Режим A — «Действие» (по умолчанию):**
  Для каждой руки берём *доминирующее* действие эталона (макс. частота). Ответ верен,
  если выбранное пользователем действие совпадает с любым действием эталона, у которого частота ≥ `threshold` (по умолчанию 1%). То есть на смешанной `A5s {3bet35/call65}` верны и 3бет, и колл.
- **Режим B — «Точные частоты»:**
  Для каждой руки сравниваем распределение; рука верна, если по каждому действию
  `|user% − ref%| ≤ tolerance` (по умолчанию ±10%).

Результаты по ячейке: `correct` ✅ / `wrong` ❌ (выбрал не то) / `missed` ⚠️ (эталон не fold, а юзер оставил fold) / `extra` (юзер поставил действие там, где эталон fold).

- **FR-13.** Подсветка ошибок прямо в матрице (рамка/иконка/затемнение).
- **FR-14.** Сводка: точность %, число correct / wrong / missed / extra, список ошибочных рук.
- **FR-15.** После Check можно переключить «overlay эталона» поверх своей раскраски для разбора.

### 6.5 Редактор диапазонов (Editor)
- **FR-16.** Режим Edit для любого спота: те же инструменты раскраски, что в тренировке, но результат сохраняется как **эталон**.
- **FR-17.** Задание частот в ячейке (несколько действий, сумма=100, авто-валидация).
- **FR-18.** Создание/переименование/удаление категорий и спотов.
- **FR-19.** Редактирование набора `actions` и их цветов per-category.
- **FR-20.** Поле заметок/рекомендаций к споту (текст с чартов FunFarm).
- **FR-21.** **Импорт/экспорт** всех данных в JSON-файл (бэкап, перенос между устройствами).
- **FR-22.** (Желательно) Импорт из формата PokerStrategy/GTO-строк диапазонов (`AA,KK,AKs:0.5,...`) — фаза 2.

### 6.6 Статистика и прогресс (фаза 2)
- **FR-23.** История попыток по спотам (дата, точность).
- **FR-24.** «Слабые руки/споты» — где чаще всего ошибки.
- **FR-25.** Режим «Spaced repetition» / случайный спот для тренировки.

---

## 7. Нефункциональные требования
- **NFR-1.** Полностью клиентское приложение, работает офлайн после загрузки.
- **NFR-2.** Данные хранятся в `localStorage`; объём диапазонов невелик (<1 МБ).
- **NFR-3.** Отзывчивость: drag-раскраска без лагов (мемоизация ячеек).
- **NFR-4.** Адаптивность: десктоп — приоритет; планшет — желательно.
- **NFR-5.** Цвета действий конфигурируемы и по умолчанию близки к чартам FunFarm.
- **NFR-6.** Локализация UI — русский (возможность добавить англ. позже).

---

## 8. Архитектура и стек

- **React 18 + TypeScript + Vite.**
- Состояние: `zustand` (или Context+reducer) — хранит ranges, выбранный спот, ответ пользователя, режим.
- Стилизация: CSS Modules или Tailwind (на выбор; матрица — CSS grid 13×13).
- Хранилище: обёртка над `localStorage` (+ seed-данные `ranges.seed.json`).
- Тесты: Vitest для логики проверки (Check) и генерации рук.

**Предлагаемая структура каталогов:**
```
poker-range-trainer/
├─ index.html
├─ package.json
├─ vite.config.ts
├─ src/
│  ├─ main.tsx
│  ├─ App.tsx
│  ├─ domain/
│  │  ├─ hands.ts          // генерация 169 рук, ID, тип (pair/suited/offsuit)
│  │  ├─ types.ts          // Action, Frequency, Spot, Category, Matrix
│  │  ├─ grading.ts        // логика Check (режимы A/B)
│  │  └─ grading.test.ts
│  ├─ store/
│  │  └─ useRangesStore.ts // zustand: данные + persist в localStorage
│  ├─ data/
│  │  └─ ranges.seed.json  // стартовые эталоны (заполняем через редактор)
│  ├─ components/
│  │  ├─ Tabs.tsx
│  │  ├─ SpotSelector.tsx
│  │  ├─ Matrix.tsx        // сетка 13×13
│  │  ├─ Cell.tsx          // одна ячейка + долевая заливка частот
│  │  ├─ ActionPalette.tsx // выбор активного действия + частота
│  │  ├─ CheckBar.tsx      // Check / Reset / Show answer / результат
│  │  ├─ ResultSummary.tsx
│  │  └─ editor/
│  │     ├─ RangeEditor.tsx
│  │     └─ ActionConfig.tsx
│  └─ styles/
└─ README.md
```

---

## 9. План реализации (по фазам)

**Фаза 0 — Каркас**
- Vite + TS проект, генерация 169 рук, отрисовка пустой матрицы 13×13, табы-заглушки.

**Фаза 1 — MVP тренировки (бинарно/действия)**
- Палитра действий, клик + drag-раскраска, режим Check (Режим A), сводка ошибок.
- 1–2 заполненных спота (напр. Открытие BTN) как seed.

**Фаза 2 — Частоты + редактор**
- Долевая заливка ячеек, задание частот, Режим B (точные частоты).
- Редактор спотов, импорт/экспорт JSON. Заполнение всех категорий §5.

**Фаза 3 — Прогресс**
- Статистика, случайный спот, слабые руки, spaced repetition.

---

## 10. Решённые вопросы по UX
1. **Заливка смешанных частот:** диагональные полосы, как на чартах FunFarm. Цвет каждой полосы = цвет действия, ширина полосы ∝ частоте. Подпись % внутри ячейки.
2. **Ввод частот в тренировке/редакторе:** слайдер на каждое действие в палитре. При изменении одного слайдера остальные доли авто-нормализуются так, чтобы сумма = 100%.
3. **Цвета действий:** точь-в-точь палитра FunFarm (см. §6.0). Зашиваем как дефолтные значения, но оставляем редактируемыми per-category.
4. **Таймер на спот:** не реализуем.
5. **Порог/толеранс** по умолчанию для Режима B: ±10% (остаётся настраиваемым).

---

## 11. Готовые эталонные данные (источник)
Чарты FunFarm (скриншоты пользователя):
- `open-range.png` — Открытие (RFI) + RFI vs 3bet
- `VPIPvs1r15-11.png` — VPIP vs 1 raiser 50bb
- `BBdef.png` — BB defense vs сайзы
- `BvB.png` — Blind vs Blind (SB/BB)
- `12_3_ISOvsFish.png` — ISO vs 1 лимпер

> Расположение оригиналов: `C:\Users\user\Desktop\Poker\Диапазоны\`
